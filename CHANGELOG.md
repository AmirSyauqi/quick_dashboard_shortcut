## 0.0.1

* Add `QuickDashboardShortcut`, a grid-based shortcut/action widget.
* Add `QuickShortcutItems` to represent each action (icon or asset image).
* Add optional badge support with configurable position/offset.
* Add asset load fallback via `errorBuilder` (shows a placeholder icon).
